<Window x:Class="EmployeeManagement.UI.DepartmentManagementWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:EmployeeManagement.UI"
        mc:Ignorable="d"
        Title="Quản lý phòng ban" 
        Height="700" Width="1200"
        WindowStartupLocation="CenterScreen">
    
    <Window.Resources>
        <Style x:Key="ButtonStyle" TargetType="Button">
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Background" Value="#2196F3"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                CornerRadius="5"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" 
                                            VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1976D2"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <!-- Header -->
        <Border Grid.Row="0" Background="#1565C0">
            <Grid Margin="20,0">
                <StackPanel VerticalAlignment="Center">
                    <TextBlock Text="QUẢN LÝ PHÒNG BAN" 
                               FontSize="24" 
                               FontWeight="Bold" 
                               Foreground="White"/>
                </StackPanel>
                <Button Content="← Quay lại" 
                        Click="BackButton_Click"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Background="#F44336"
                        Foreground="White"
                        Padding="15,8"
                        BorderThickness="0"
                        Cursor="Hand">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border Background="{TemplateBinding Background}" 
                                    CornerRadius="5">
                                <ContentPresenter HorizontalAlignment="Center" 
                                                VerticalAlignment="Center"
                                                Margin="10,5"/>
                            </Border>
                        </ControlTemplate>
                    </Button.Template>
                </Button>
            </Grid>
        </Border>
        
        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="20">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="1.5*"/>
                <ColumnDefinition Width="15"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>
            
            <!-- Danh sách phòng ban -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Danh sách phòng ban" 
                               FontSize="18" 
                               FontWeight="Bold"
                               VerticalAlignment="Center"/>
                    <Button Content="🔄" 
                            Click="RefreshButton_Click"
                            Style="{StaticResource ButtonStyle}"
                            Background="#4CAF50"
                            Margin="10,0,0,0"
                            Padding="10,5"/>
                </StackPanel>
                
                <DataGrid Grid.Row="1"
                          x:Name="dgDepartments"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          SelectionChanged="DgDepartments_SelectionChanged"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          AlternatingRowBackground="#F5F5F5">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" 
                                          Binding="{Binding DepartmentId}" 
                                          Width="50"/>
                        <DataGridTextColumn Header="Tên phòng ban" 
                                          Binding="{Binding DepartmentName}" 
                                          Width="*"/>
                        <DataGridTextColumn Header="Số NV" 
                                          Binding="{Binding Employees}" 
                                          Width="70"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>
            
            <!-- Danh sách nhân viên trong phòng ban -->
            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                
                <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,10">
                    <TextBlock Text="Nhân viên trong phòng ban" 
                               FontSize="18" 
                               FontWeight="Bold"
                               VerticalAlignment="Center"/>
                </StackPanel>
                
                <DataGrid Grid.Row="1"
                          x:Name="dgEmployeesInDepartment"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          SelectionMode="Single"
                          CanUserAddRows="False"
                          CanUserDeleteRows="False"
                          GridLinesVisibility="Horizontal"
                          HeadersVisibility="Column"
                          AlternatingRowBackground="#F5F5F5">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" 
                                          Binding="{Binding EmployeeId}" 
                                          Width="50"/>
                        <DataGridTextColumn Header="Họ tên" 
                                          Binding="{Binding FullName}" 
                                          Width="*"/>
                        <DataGridTextColumn Header="Email" 
                                          Binding="{Binding Email}" 
                                          Width="150"/>
                    </DataGrid.Columns>
                </DataGrid>
                
                <!-- Nút gán nhân viên -->
                <Button Grid.Row="2"
                        Content="👥 Gán nhân viên vào phòng ban"
                        Click="AssignEmployeeButton_Click"
                        Style="{StaticResource ButtonStyle}"
                        Background="#9C27B0"
                        Margin="0,10,0,0"
                        HorizontalAlignment="Stretch"/>
            </Grid>
            
            <!-- Form thêm/sửa phòng ban -->
            <Border Grid.Column="4" 
                    BorderBrush="#DDD" 
                    BorderThickness="1" 
                    CornerRadius="5"
                    Padding="15"
                    Background="White">
                <StackPanel>
                    <TextBlock Text="Thông tin phòng ban" 
                               FontSize="16" 
                               FontWeight="Bold"
                               Margin="0,0,0,15"/>
                    
                    <TextBlock Text="ID:" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtDepartmentId" 
                             IsReadOnly="True"
                             Background="#F5F5F5"
                             Padding="8"
                             Margin="0,0,0,10"/>
                    
                    <TextBlock Text="Tên phòng ban:" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtDepartmentName" 
                             Padding="8"
                             Margin="0,0,0,10"/>
                    
                    <TextBlock Text="Số nhân viên:" Margin="0,0,0,5"/>
                    <TextBox x:Name="txtEmployeeCount" 
                             IsReadOnly="True"
                             Background="#F5F5F5"
                             Padding="8"
                             Margin="0,0,0,20"/>
                    
                    <!-- Buttons -->
                    <Button Content="➕ Thêm mới" 
                            Click="AddButton_Click"
                            Style="{StaticResource ButtonStyle}"
                            Background="#4CAF50"/>
                    
                    <Button Content="💾 Cập nhật" 
                            Click="UpdateButton_Click"
                            Style="{StaticResource ButtonStyle}"
                            Background="#FF9800"/>
                    
                    <Button Content="🗑️ Xóa" 
                            Click="DeleteButton_Click"
                            Style="{StaticResource ButtonStyle}"
                            Background="#F44336"/>
                    
                    <Button Content="🔄 Làm mới form" 
                            Click="ClearButton_Click"
                            Style="{StaticResource ButtonStyle}"
                            Background="#607D8B"/>
                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
